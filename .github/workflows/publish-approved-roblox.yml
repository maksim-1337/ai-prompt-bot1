name: Legacy photo publisher (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy publication disabled. Only approved Markdown drafts may publish.'
