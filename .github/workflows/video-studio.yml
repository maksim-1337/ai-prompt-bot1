name: NewsLight Video Studio (paused)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Channel previews disabled. Use Markdown editorial review.'
